import { createStyles, makeStyles } from '@material-ui/core';

const useStyles = makeStyles(() =>
  createStyles({
    root: {
      minWidth: '452px',
      width: 'auto',
    },
  })
);

export { useStyles };
